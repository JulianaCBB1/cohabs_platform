services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: cohabs
      POSTGRES_PASSWORD: cohabs
      POSTGRES_DB: platform
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
